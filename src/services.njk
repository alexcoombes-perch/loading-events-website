---
layout: base.njk
permalink: /services/
eleventyComputed:
  page_title: "{{ services.page_title }}"
---
<section class="section--tight">
  <div class="container">
    <span class="eyebrow">What I offer</span>
    <h1>{{ services.heading }}</h1>
    <div class="prose">
      {% for para in services.intro_paragraphs %}
      <p>{{ para }}</p>
      {% endfor %}
    </div>

    <div class="services-grid">
      {% for item in services.services %}
      <div class="service-card reveal reveal--{{ loop.index }}">
        <h3>{{ item.name }}</h3>
        <p>{{ item.description }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{% if services.accent_image %}
<div class="container reveal">
  <img src="{{ services.accent_image }}" alt="{{ services.accent_image_alt }}" style="border-radius: 14px; max-height: 480px; width: 100%; object-fit: cover; box-shadow: var(--shadow-soft);">
</div>
{% endif %}

{% if services.photo_moment %}
<section class="photo-moment section--flush" style="background-image: url('{{ services.photo_moment.image }}');" role="img" aria-label="{{ services.photo_moment.image_alt }}">
  <p class="photo-moment__quote reveal script">{{ services.photo_moment.quote }}</p>
</section>
{% endif %}

{% if services.faq %}
<section class="section--tight">
  <div class="container reveal">
    <span class="eyebrow">Good to know</span>
    <h2>A few questions couples ask</h2>
    <div class="faq-list">
      {% for item in services.faq %}
      <div class="faq-item">
        <h3>{{ item.question }}</h3>
        <p>{{ item.answer }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}

<section class="section--blue text-center">
  <div class="container reveal">
    <p class="script" style="font-size: 1.9rem; color: var(--white);">{{ services.closing_line }}</p>
    <a class="btn btn--outline" href="{{ services.cta_url }}">{{ services.cta_label }}</a>
  </div>
</section>
